암호 -대칭키, 비대칭키
해쉬
public key Infrastructure

■해킹
-1990년대
윈도우 NT 3.1 서버 - 웹, 시스템 => 중앙화 서버
아메리카온라인 해킹
트로이 목바, 백 오리피스
- 2000년대
분산 서비스 거부 공격
웜과 바이러스 공격
개인 정보 유출과 도용
전자상거래 교란
APT 공격의 등장
-2010년대
농협 사이버 테러 ( 내부에서 데이터 탈취 및 수정)
스마트폰 해킹 ( 문자, APK -> email 셀프 설치 )


//
■보안의 3대 요소
- 기밀성(confidentiality)
방화벽, 암호, 패스워드
- 무결성(integrity)
권한을 가진 사용자만 변경가능 하도록 하는 것.
-가용성(availability)
필요한 시점에 정보 자산에 대한 접근이 가능하도록 하는 것.
// 블록체인은 무결성, 가용성은 우수하나 기밀성은 떨어진다.

중앙화된 서버의 단점, 블록체인으로 단점 보완 가능
■서비스 거부 공격(DoS와 DDoS)
-서비스 거부 공격(DoS)
자원 고갈 공격형	( 바이러스 감염 시켜 좀비 피시로 만듬)
-분산 서비스 거부 공격(DDoS)
분산화 DoS


해결법 => DATA 분산화(블록체인), DMZ( 중앙화 서버를 고도화된 필터링)

■블록체인에 필요한 보안기술
-암호
 	(keyA)암호화 => 복호화(keyA)
		AES 대칭키( 속도 높음, 효율 높음), 키를 나눠가져야함( 대칭키 분배의 문제 ) AP, WIFI
	(keyX)암호화 => 복호화(keyY)
		RSA, ECDSA비대칭키( Public key, private key) 소인수 분해, 타원곡선 방정식, 한 방향은 쉽게 키를 만들고
										또 다른 방향은 매우 어렵게 만들 수 있도록 한다.
-서명
-암호 통신
-해쉬	- 데이터가 변경되었음을 알려주는 hash값 (알고리즘으로 인해)
-공개키인증구조(private키로 증명 => public B/C)PKI

-대칭 암호화 방식
AES 알고리즘
SEED 알고리즘
ARIA 알고리즘
양자 암호

-비대칭 암호화 방식( 암호 통신, 비대칭- 암호통신, 서명 )
기밀성
부인 방지(nonrepudiation)

상대방의 공개키로 암호화를 하여 전달 하고 받은 문서는 공개키로 복호화를 하여 본다.
상대방의 공개키로 암호화하여 문서를 보내고 상대방은 자신의 개인키로 복호화 하여 문서를 본다.
공개키를 요청하여 암호화 한다.

영희(pub,pri) => 철수(pub,pri)
	- 영희가 철수에게 공개키 요청( if pub키 가로체기 -> 가짜 pub키 보냄)[ 철수는 복호화 불가능, 가짜 키 보낸 사람만 복호화 가능 ]
					- 해결책 : CA서명으로 pub를 등록하여 보냄( 검증을 요청해야하기에 가로체기 불가능 )
//

-해쉬
문자열을 더 짧은 길이의 값이나 키로 변환
위조 변조를 확인 하기  위한것
데이터베이스의 탐색을 효과적으로 구현(DB검색 기술)
종류 ( SHA[비트코인]
	- 해쉬 값은 절대적으로 유추할 수 없어야 한다.
블록 해쉬값이 특정 숫자보다 작아지게 하는 nonce값을 찾는 것이 마이닝
16진수를 주로 사용(Hexa)
_ _ _ _ / _ _ _ _
숫자에 영문자를 대입
10110100 =>kd
abcdefghijklmn

